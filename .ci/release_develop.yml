version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      # TODO sign commits and tags
      - git init
      - git remote add origin https://${GITHUB_ACCESS_TOKEN}@github.com/micov6/go-argparse.git
      # TODO point to develop
      - git pull origin pull_request
      - ./scripts/install_github_cli.sh
  build:
    commands:
      # TODO point to develop
      - ./scripts/release pull_request
